<h1>Your Favourite Restaurants</h1>
<%= link_to "Add a new restaurant!", new_restaurant_path, class:"btn btn-success m-3" %>

<% @restaurants.each do |restaurant| %></a>
  <h2><%= restaurant.name%></h2>
  <p>Address: <%= restaurant.address %></p>
  <p>Phone Number: <%= restaurant.phone_number %></p>
  <%= link_to 'Check Restaurant', restaurant_path(restaurant), class: "btn btn-primary" %>
<% end %>
